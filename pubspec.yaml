name: sa2e7
description: "A new Flutter project."


version: 1.0.0+1

environment:
  sdk: ^3.7.2


dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  google_nav_bar: ^5.0.7
  cached_network_image: ^3.2.3
  smooth_page_indicator: ^1.1.0
  shared_preferences: ^2.2.2
  image_picker: ^1.2.1
  reorderables: ^0.5.0
  provider: ^6.0.5
  like_button: ^2.0.4
  lottie: ^3.1.0
  cloud_firestore: ^4.17.2
  firebase_core: ^2.30.1
  firebase_auth: ^4.5.0
  firebase_storage: ^11.7.4
  google_sign_in: ^6.1.0
  google_maps_flutter: ^2.14.0
  permission_handler: ^11.3.0
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  http: ^1.5.0 # for calling Directions API
  geolocator: ^12.0.0
  url_launcher: ^6.1.10
  geocoding: ^2.0.5
  flutter_polyline_points: ^2.0.0








dev_dependencies:
  flutter_test:
    sdk: flutter


  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/lottie/Compass.json
    - assets/lottie/Map.json
    - assets/image/onboarding3.png
    - assets/image/beirut.jpeg
    - assets/image/byblos.jpeg
    - assets/image/baalbek.jpeg
    - assets/image/cedars.jpeg
    - assets/image/jeita.jpg
    - assets/image/tyre.jpeg
    - assets/image/lebhero.png
  #  assets/image/paragliding.jpg
  #  assets/image/cedars_hike.jpg
  #  assets/image/wine_tour.jpg
  #  assets/image/rafting.jpg
  #  assets/image/lebanon_hero.jpg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
